# GENERATED BY JERRY vdev FROM .jerry/review/ — DO NOT EDIT.
# Edit the spec and run `jerry generate`. CI fails on drift.
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Jerry
        run: curl -sSL https://jerry.dev/install.sh | sh -s -- --version vdev
      - name: Install pi
        run: npm install -g @mariozechner/pi-coding-agent@0.73.1
      - name: Drift check
        run: jerry generate --check
      - name: review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: jerry exec review/review
      - name: report
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: jerry exec review/report
      - name: gate
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: jerry exec review/gate
