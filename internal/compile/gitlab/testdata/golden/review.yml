# GENERATED BY JERRY v0.1.0 — DO NOT EDIT.
# Edit the spec and run `jerry generate --backend gitlab`. CI fails on drift.

stages:
  - preamble
  - review-0-review
  - review-1-report
  - review-2-gate

jerry-install:
  stage: preamble
  variables:
    GIT_DEPTH: 0
  script:
    - curl -sSL https://jerry.dev/install.sh | sh -s -- --version v0.1.0
    - npm install -g @mariozechner/pi-coding-agent@0.73.1
    - jerry generate --check

jerry-review-review:
  stage: review-0-review
  needs: ["jerry-install"]
  variables:
    GIT_DEPTH: 0
    ANTHROPIC_API_KEY: $ANTHROPIC_API_KEY
    GITHUB_TOKEN: $GITHUB_TOKEN
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - jerry exec review/review

jerry-review-report:
  stage: review-1-report
  needs: ["jerry-review-review"]
  variables:
    GIT_DEPTH: 0
    ANTHROPIC_API_KEY: $ANTHROPIC_API_KEY
    GITHUB_TOKEN: $GITHUB_TOKEN
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - jerry exec review/report

jerry-review-gate:
  stage: review-2-gate
  needs: ["jerry-review-report"]
  variables:
    GIT_DEPTH: 0
    ANTHROPIC_API_KEY: $ANTHROPIC_API_KEY
    GITHUB_TOKEN: $GITHUB_TOKEN
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - jerry exec review/gate

