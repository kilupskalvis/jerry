# GENERATED BY JERRY v0.1.0 FROM .jerry/minimal/ — DO NOT EDIT.
# Edit the spec and run `jerry generate`. CI fails on drift.
on:
  push:
    branches: [main]
jobs:
  minimal:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Jerry
        run: curl -sSL https://jerry.dev/install.sh | sh -s -- --version v0.1.0
      - name: Drift check
        run: jerry generate --check
      - name: greet
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: jerry exec minimal/greet
