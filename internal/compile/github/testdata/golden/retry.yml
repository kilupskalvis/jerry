# GENERATED BY JERRY v0.1.0 FROM .jerry/retry/ — DO NOT EDIT.
# Edit the spec and run `jerry generate`. CI fails on drift.
on:
  push:
jobs:
  retry:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Jerry
        run: curl -sSL https://jerry.dev/install.sh | sh -s -- --version v0.1.0
      - name: Drift check
        run: jerry generate --check
      - name: flaky
        timeout-minutes: 11
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          attempt=0
          max_attempts=3
          while [ "$attempt" -lt "$max_attempts" ]; do
            attempt=$((attempt + 1))
            jerry exec retry/flaky && exit 0
            rc=$?
            if [ "$rc" -eq 2 ] || [ "$rc" -eq 4 ]; then exit "$rc"; fi
            if [ "$attempt" -lt "$max_attempts" ]; then
              echo "Retry $attempt/$max_attempts (exit $rc)"
            else
              exit "$rc"
            fi
          done
