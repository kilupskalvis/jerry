# Generated by motif init — edit triggers and secrets as needed.
name: Motif
on:
  issues:
    types: [opened, labeled]
  workflow_dispatch:
    inputs:
      intent:
        description: 'What to build'
        required: true

jobs:
  generate:
    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.issue.labels.*.name, 'motif')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: read
    steps:
      - uses: motif-protocol/runner@v1
        with:
          pipeline: feature
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
